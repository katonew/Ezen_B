- 자바에는 다양한 API
- java.base 모듈
	- java.lang
		-object 클래스
			- 자바 클래스의 최상위 클래스
			- 자바의 모든 클래스는 Object 자식이거나 자속 클래스
			
			-메소드
				1. 객체명A.equals(객체명B) 
									- 두 객체의 주소비교
									- 객체 내부 비교시 오버라이딩
				2. 객체명.hashCode()	: 객체의 주소값으로 해시코드 생성해서 정수로 반환
				3. 객체명.toString()	: 객체의 정보반환
									  [*print()에서 객체명 == 객체명.toString()]
				 
		- System 클래스
			- 자바는 운영체제에 접근하기 어렵다.
			- System 클래스는 일부의 운영체제에 접근 가능
			
			- 필드
				System.out				: 콘솔에 문자 출력
				System.err				: 콘솔에 에러내용 출력
				System.in				: 키보드 입력
					System.in.read()	: 엔터단위 읽기 [ * 예외처리 필수]
			- 메소드
				System.exit(0)
						*break;			: 반복문 {} 탈출
						*return;		: 함수 { } 탈출
						*System.exit(0)	: 프로그램 강제 종료
				System.nanoTime();		: 현재 시간을 나노초로 표현
				System.getProperty(속성명): 속성명의 시스템 정보
				
		- String 클래스
			